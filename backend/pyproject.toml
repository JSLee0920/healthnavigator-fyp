[project]
name = "healthnavigator-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain-huggingface>=1.2.1",
    "xmltodict>=1.0.4",
]
